#include "execute.h"

#define ERROR_CODE 127

int execute(char *file_path, char *argv[])
{
    (void) file_path;
    (void) argv;

    // remember to return ERROR_CODE on error.
    return ERROR_CODE;
}
